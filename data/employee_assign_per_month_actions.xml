<odoo>
    <record id="action_add_item" model="ir.actions.server">
        <field name="name">Add Item</field>
        <field name="model_id" ref="model_employee_assign_per_month"/>
        <field name="state">code</field>
        <field name="code">action = env['employee_assign_per_month'].add_item()</field>
    </record>
</odoo>
